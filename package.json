{
  "name": "image-to-pdf-app",
  "version": "1.0.0",
  "description": "Image to PDF app (server/web portion)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "lint": "eslint . --ext .ts,.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "typescript": "^5.1.0",
    "ts-node-dev": "^2.0.0",
    "@types/express": "^4.17.17"
  }
}
